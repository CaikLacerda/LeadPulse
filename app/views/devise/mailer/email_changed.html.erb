<p>Olá, <%= @email %>.</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>Seu e-mail está sendo alterado para <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p>Seu e-mail foi alterado para <%= @resource.email %>.</p>
<% end %>
